import React from "react";
import { View } from "react-native";

export const Hr = () => {
  return (
    <View
      style={{
        borderBottomColor: 'black',
        borderWidth: 0.4,
        width: "100%"
      }}
    />
  );
};